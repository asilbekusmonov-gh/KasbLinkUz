1. Celery + Redis
   ☐ Setup Celery
   ☐ Welcome email on register
   ☐ Email on order accept/complete/cancel

2. File Uploads
   ☐ Portfolio images
   ☐ Service cover image
   ☐ Profile image

3. Deploy
   ☐ Push to GitHub
   ☐ Deploy to Railway or Render
   ☐ Get a live URL
   ☐ Update README with live URL
   ← DO THIS EARLY. Don't wait until the end.

4. Tests
   ☐ pytest setup
   ☐ Test your validations
   ☐ Test order flow
   ☐ Test permissions

5. Docker
   ☐ Dockerfile
   ☐ docker-compose.yml
   ☐ Run entire project with one command

6. GitHub Actions CI
   ☐ Run tests automatically on every push

7. Real-time Chat
   ☐ Django Channels
   ☐ WebSocket
   ☐ Replace basic REST chat

8. Polish
   ☐ Clean up README
   ☐ Architecture diagram
   ☐ Consistent error messages
   ☐ Final Postman test of everything









   ---------------
ishbor

  category da detailni olib tashlash
  @property qoshish function bilan(is_worker)
  get_queryset da qs foydalanish
  hidden dan foydalanish client uchun
  action da Order.status.Pending
  base class dan foydalanish date time larga inherit qilish
  ViewSet dan togri foydalanish
  permissionda actionni orniga  | , and dan foydalanish(drf action permission)
  update da id ni olib tashlash


Reviewing weaker sides of KasbLInk

worker cant rate his own rating(POST)
completed_orders_count ni qolda kiritb bolmaydi(qachon order completed bolsagina completed_orders_countga +1)
service da min_price kichik boishi kerak max_price dan
update qilayotganda aftamatski ozinikini update qilsin, id kiritsh soralmasin










test
test coverage
~80% >


vertical horizontal scaling



pytest pytest coverage
isort flake8



--------------------


.venv/bin/python manage.py runserver ----> backend run



# Go to the frontend folder
cd frontend

# Activate Node.js environment
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# Start the Next.js development server
npm run dev


cd frontend
npm run dev


http://localhost:3000




